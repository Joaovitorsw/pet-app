import { FormControlRangeDirective } from './form-control-range.directive';

describe('FormControlRangeDirective', () => {
  it('should create an instance', () => {
    const directive = new FormControlRangeDirective();
    expect(directive).toBeTruthy();
  });
});
